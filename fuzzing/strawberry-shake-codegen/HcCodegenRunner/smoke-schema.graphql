schema { query: Query }
type Query { me: User node(id: ID!): Node }
interface Node { id: ID! }
type User implements Node { id: ID! name: String! avatar: Image }
type Image { url: String! width: Int height: Int }
